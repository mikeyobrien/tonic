defmodule Demo do
  def run() do
    case {:ok, 42} do
      {:ok, val} when val > 0 -> "positive"
      {:ok, _} -> "zero or negative"
      {:error, _reason} -> "error"
      _ -> "other"
    end
  end
end
