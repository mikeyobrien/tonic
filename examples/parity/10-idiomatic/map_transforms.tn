defmodule Demo do
  def run() do
    {get_name(%{name: "Alice", age: 30}),
     get_updated_age(%{name: "Alice", age: 30})}
  end

  defp get_name(user) do
    user.name
  end

  defp get_updated_age(user) do
    case %{user | age: 31} do
      updated -> updated.age
    end
  end
end
