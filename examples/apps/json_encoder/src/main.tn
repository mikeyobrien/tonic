defmodule Demo do
  def run() do
    IO.puts(JsonEncoder.encode(%{
      name: "Tonic",
      version: 1,
      features: ["pattern matching", "pipe chains", "recursion"],
      metadata: %{stable: false, stars: 42},
      license: nil
    }))
  end
end
